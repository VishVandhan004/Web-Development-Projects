// This is the main file of the frontend
// This is the file where the react app is rendered
import './App.css'
// the below is the App component
// it is a functional component
function App() {
  return (
    // only one parent element is allowed in the return statement
    // the below is the parent element
    // the below is the fragment, it is used to handle multiple divs without a parent div
    <>
      <div>
        
      </div>
    </>
  )
}

export default App
